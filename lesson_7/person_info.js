function solve(fName, lName, age) {
    let personInfo = { firstName: `${fName}`, lastName: `${lName}`, age: `${age}` };
    // return console.log(`firstName: ${personInfo.firstName}\nlastName: ${personInfo.lastName}\nage ${personInfo.age}`);
    return personInfo;
}

solve("Peter", "Pan", "20")